import classNames from "classnames";

const Header = () => {
  return <header className={classNames("sticky w-full h-12 p-2 bg-neutral-50 shadow", "flex flex-row items-center")}>header</header>;
};

export default Header;
